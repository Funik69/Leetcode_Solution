class Solution{
	public:
	vector<long long> allNumbers(long long A,long long B)
	{
	    vector<long long > v;
	    long long store=A;
	    while(A<=B){
	        if(B%A==0)
	        v.push_back(A);
	        A+=store;
	    }
	    return v;
	}
};
